# KlarText Frontend Environment Variables
# Copy this file to .env.local for local development

# API URL - Backend endpoint for text simplification
# Local development (default if not set):
VITE_API_URL=http://localhost:8000

# Production (Fly.io deployment):
# VITE_API_URL=https://klartext-api.fly.dev

# Note: API key is obtained dynamically after password authentication
# No VITE_API_KEY needed - the key is returned by the backend on successful login
