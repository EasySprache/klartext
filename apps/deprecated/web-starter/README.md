# KlarText Web Application (Deprecated)

A Next.js web application for the KlarText text simplification project.

## Prerequisites

- **Node.js** (v18 or higher recommended)
- **npm** (comes with Node.js)

---

## Step-by-Step Instructions

### 📁 Step 1: Open a Terminal Window

1. Open the **Terminal** application on your Mac
   - You can find it in `Applications > Utilities > Terminal`
   - Or use Spotlight: press `⌘ + Space`, type `Terminal`, and press Enter

---

### 📂 Step 2: Navigate to the Web App Directory

In the terminal, run this command to navigate to the web application folder:

```bash
cd apps/deprecated/web-starter
```

---

### 📦 Step 3: Install Dependencies

Run this command to install all required packages:

```bash
npm install
```

> ⏳ This may take a minute or two the first time. Wait until you see a message indicating installation is complete.

---

### 🚀 Step 4: Start the Development Server

Run this command to start the web application:

```bash
npm run dev
```

You should see output similar to:
```
▲ Next.js 14.2.5
- Local:        http://localhost:3000
```

> ✅ **Keep this terminal window open!** The server will keep running here.

---

### 🌐 Step 5: Open the Application in Your Browser

1. Open your web browser (Safari, Chrome, Firefox, etc.)
2. Go to: **http://localhost:3000**

---

## Quick Reference

| Step | Action | Command/Location |
|------|--------|------------------|
| 1 | Open Terminal | Applications > Utilities > Terminal |
| 2 | Navigate to folder | `cd apps/deprecated/web-starter` |
| 3 | Install packages | `npm install` |
| 4 | Start server | `npm run dev` |
| 5 | View in browser | http://localhost:3000 |

---

## Stopping the Server

When you're done, go back to the terminal window where the server is running and press `Ctrl + C` to stop it.

---

## Available Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Start development server on port 3000 |
| `npm run build` | Build for production |
| `npm run start` | Start production server on port 3000 |
| `npm run lint` | Run ESLint checks |
