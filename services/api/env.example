# KlarText API Environment Variables
# Copy this file to .env and fill in your values:
#   cp env.example .env
# (.env is gitignored and will not be committed)

# =============================================================================
# Required: LLM Provider
# =============================================================================
# Get your Groq API key from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_key_here

# =============================================================================
# Optional: TTS Provider (Text-to-Speech)
# =============================================================================
# Options: gtts (default), openai, google
TTS_PROVIDER=gtts

# For OpenAI TTS (higher quality)
# OPENAI_API_KEY=your_openai_key_here

# For Google Cloud TTS (best quality)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# =============================================================================
# Optional: Application Configuration
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=INFO

# CORS: Comma-separated list of allowed origins (for frontend)
# Development:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:7860
# Production (set via Fly.io secrets):
# ALLOWED_ORIGINS=https://your-app.vercel.app

# =============================================================================
# Optional: API Behavior
# =============================================================================
# Max text length for simplification (characters)
# MAX_TEXT_LENGTH=5000

# Max PDF file size (MB)
# MAX_PDF_SIZE_MB=10

# LLM temperature (0.0-1.0, lower = more consistent)
# LLM_TEMPERATURE=0.3

# =============================================================================
# Deployment Configuration (Fly.io)
# =============================================================================
# PORT is set automatically by Fly.io (typically 8080)
# For local development, uvicorn defaults to 8000
# PORT=8080

# Custom templates directory (optional, defaults to ./prompts/templates)
# TEMPLATES_DIR=/app/prompts/templates

# =============================================================================
# API Security (Production)
# =============================================================================
# API key for authenticating frontend requests
# Required in production - set via: fly secrets set API_KEY=your_secure_key
# In development, leave unset to allow all requests
# API_KEY=your_secure_api_key_here
